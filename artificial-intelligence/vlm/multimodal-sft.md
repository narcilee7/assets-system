# 多模态指令微调

多模态 SFT 是将预训练好的 VLM 变成能遵循指令、回答问题的助手的关键步骤。

## 数据格式

### 对话格式

```json
{
  "image": "path/to/image.jpg",
  "conversations": [
    {"from": "human", "value": "<image>\n这张图片里有什么？"},
    {"from": "gpt", "value": "图片中有一只橘色的猫坐在沙发上..."},
    {"from": "human", "value": "猫在做什么？"},
    {"from": "gpt", "value": "它正在打盹，眼睛半闭着。"}
  ]
}
```

### 关键设计

| 设计 | 说明 |
|------|------|
| `<image>` token | 标记图像插入位置 |
| 系统提示 | 定义模型角色和能力 |
| 多轮对话 | 支持上下文引用 |

## 数据类型

| 类型 | 内容 | 占比建议 |
|------|------|---------|
| 描述 (Caption) | 图像整体描述 | 20% |
| VQA | 视觉问答 | 30% |
| OCR | 文字识别和文档理解 | 15% |
| 推理 | 多模态逻辑推理 | 15% |
| 对话 | 多轮交互 | 10% |
| 拒绝 (Rejection) | 无法回答的情况 | 10% |

## 关键能力训练

### OCR 与文档理解

```
挑战：
- 密集文本（小字、表格、公式）
- 布局信息（段落、标题、列表）
- 多语言混合

解决：
- 高分辨率输入（672×672+）
- 切图策略（将文档分块）
- 专门的文档预训练数据
```

### Grounding

```
任务：让模型指出来"在哪里"

格式：
用户：指出图中所有的猫
模型：<bbox>猫1在[100, 200, 300, 400]</bbox>，
      <bbox>猫2在[500, 150, 700, 350]</bbox>

坐标编码：
- 绝对坐标：[x1, y1, x2, y2]
- 相对坐标：归一化到 0-1000
- 连续 token：将坐标量化为 token
```

### 视频理解

```
挑战：
- 时序信息（动作、变化）
- 帧数选择（均匀采样 vs 关键帧）

策略：
- 均匀采样 8-32 帧
- 每帧独立编码，加上时间位置编码
- 或用视频专用编码器（如 Video Swin）
```

## 训练技巧

### 损失掩码

```
只计算 assistant 回复的 loss，不计算 human 输入的 loss：

<image> 图中有什么？ 有一只猫。 它是什么颜色？ 橘色的。
[MASK]  [MASK]  [MASK]  [计算]  [MASK]  [MASK]  [计算]
```

### 学习率策略

| 组件 | 学习率 | 是否冻结 |
|------|--------|---------|
| Vision Encoder | 0 或很小 | 通常冻结 |
| Projector | 2e-4 | 训练 |
| LLM | 2e-5 ~ 1e-5 | 训练（大模型可冻结深层） |

### 数据平衡

```
问题：不同类型数据量差异大
解决：
- 按类型分组，每组内 shuffle
- 采样时按目标比例混合
- 或使用重复采样（upsampling）
```

## 评估

| 基准 | 能力 | 代表模型分 |
|------|------|-----------|
| MMMU | 大学级别多学科推理 | 30-50% |
| MMBench | 综合能力 | 70-80% |
| SEED-Bench | 图像理解 | 70-80% |
| OCR-Bench | OCR 和文档 | 60-80% |
| MathVista | 数学推理 | 40-60% |

## 常见误区

| 误区 | 正解 |
|------|------|
| 多模态数据越多越好 | ❌ 质量比数量重要，垃圾数据降低性能 |
| 所有任务一起训最好 | ⚠️ 需要课程学习，简单到复杂 |
| 视觉能力只靠 SFT | ❌ 预训练对齐阶段同样关键 |
| 高分辨率总是更好 | ⚠️ 是，但训练成本和显存显著增加 |

## 快速检查清单

- [ ] 理解多模态对话数据的格式
- [ ] 知道主要的多模态能力类型和训练要点
- [ ] 理解损失掩码的设计
- [ ] 知道主要的多模态评估基准
